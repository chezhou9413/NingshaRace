# 开局、界面与资源修复提交记录

日期：2026-09-05。目标分支：`main`。本记录覆盖上一次“UI优化”提交之后尚未提交的工作，不重复归入已在历史中的地牢算法、祭坛任务等实现。

## 开局与人物生成

- “深砂遗民”先建立沙漠地表入口，再生成相连的地下巨坑家园；通过专用场景部件直接安置三名成员与物资，不走厚岩顶受限的空投选点。
- 地下家园使用专用生态、稳定厚岩顶和24℃基础温度；入口、离洞绳与家园双向绑定，安置区保留可达道路和揭雾。
- 开局人物使用凝砂族背景覆盖过滤器，避免与原版玩家派系背景合并。
- 协调凝砂族儿童的年龄范围与生成请求阶段，避免默认成人阶段与儿童年龄上限冲突产生300次重试；明确指定年龄与孵化流程保留原意。

## 界面与资源

- 以原有静态砂岩、细沙与边缘积沙为主体，叠加稀薄的全幅流沙，保留原配色、文字、图标与边框布局。
- 动态细沙采用自由分布、不同速度与噪声扰动，使用共享画面和纹理缓存；切换游戏统一释放相关资源。
- 文字增加细窄暗影，保持原有裁剪、换行与交互区域。
- 使用用户提供的香料、蘑菇饼、炙热地煲和两种分辨率的祭坛图；祭坛地图贴图与菜单图标分别接入，不改玩法数值。
- 将 UI Shader、包含文件、构建器及导入信息纳入 `SourceAssets/UI`，同时提交双平台 UI 资源包与清单，避免源码只留在本地 Unity 工程。

## 线程与文档

- 为货运组件与命令图标缓存标记启动主线程预加载，避免后台开局实例化入口时加载 Unity 资源。
- 保留原版错误报告，不增加异常吞噬、重复回退、地图并行修改或旧存档迁移。
- 更新开局、儿童生成、界面、祭坛料理的开发文档及人工验收入口。

## 验证与交付边界

- Release 编译通过；现有 Harmony 引用的 MSIL/AMD64 架构警告保留。
- Windows、macOS UI 资源包已构建，清单包含底材与流沙 Shader，无外部资源包依赖。
- 新贴图与用户源文件核对一致，XML 纹理引用有效；执行文本编码、XML 解析和 `git diff --check` 静态检查。
- 未启动 RimWorld，也未进行视觉或游戏流程运行时验收。相关条目保持待手工状态。
- 本次推送不部署到游戏目录。加载本轮 DLL、贴图和资源包需要完整重启游戏；单纯读档不能重新执行静态图标初始化。

Git 提交信息采用“修复地下开局与资源线程加载，完善流沙界面和正式贴图”，并按上述模块说明具体内容。
